## YAML Template.
server:
    port:8888

spring:
    application:
        name: myvalue
    cloud:
        config:
            server:
            git:
                uri: https://github.com/manuelcepero/configserver
                default-label: master
                search-paths:
                    - '*service'